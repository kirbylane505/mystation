export const metadata = {
  title: "QR Codes - Print & Share",
  description:
    "Printable QR codes for MyStation. Use at events, shows, and LOTL.",
  robots: { index: false, follow: false },
};

export default function QRLayout({ children }) {
  return children;
}
