# MyStation Free Play Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Remove the 30-second preview gate so ALL users can listen to full tracks for free. Keep email gate, keep $4.99 sub with perks, keep $14.99 creator tier. Add kill switch to re-enable gate at 10K users.

**Architecture:** Single change in `playerStore.js` — make `isPreviewOnly()` return false always. Add `FREE_PLAY_MODE` flag for easy rollback. AudioPlayer, token endpoint, and subscribe modals remain intact but won't trigger during free play.

**Tech Stack:** Next.js, Zustand (playerStore)

---

### Task 1: Enable Free Play Mode

**Files:**
- Modify: `src/store/playerStore.js:358-400`

**Step 1: Add FREE_PLAY_MODE flag and update isPreviewOnly**

Change `isPreviewOnly()` to check a kill switch flag. When `FREE_PLAY_MODE = true`, all tracks play full length.

```js
// FREE PLAY MODE — set to false to re-enable 30s preview gate (target: 10K users)
const FREE_PLAY_MODE = true;

// Specific free tracks — these ALWAYS play full regardless of mode
const FREE_TRACK_IDS = [500, 501];

/**
 * isPreviewOnly — returns true if this track should be limited to 30-second preview.
 * When FREE_PLAY_MODE is true, ALL tracks play full for everyone.
 */
export function isPreviewOnly(track) {
  // FREE PLAY MODE — everyone listens free
  if (FREE_PLAY_MODE) return false;

  if (!track?.id) return false;
  // ... rest of existing logic stays for when gate comes back
}
```

**Step 2: Verify and deploy**

Run: `cd /Users/impossibledreamzmusicgroup/MikePageEmpire/apps/mystation && npx next build`
Expected: Build succeeds, 0 errors

**Step 3: Commit**

```bash
git add src/store/playerStore.js
git commit -m "feat: enable free play mode — all tracks full length for all users"
```
